// The World-Lab: the ?sim (default) construct-and-place bench. Stubbed here so
// the app compiles and routes correctly ahead of Task 4, which fleshes this
// out into the real starter/build/roster UI.
export function startWorldLab(): void {}

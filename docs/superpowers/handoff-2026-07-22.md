# Wonder — handoff (resume here) · 2026-07-22 (plant/insect ecology — built, QA'd ×3, **not yet pushed**)

Supersedes `handoff-2026-07-21-playtest.md`. This session (2026-07-21 → 07-22, Opus +
Blaine live, Fable in worktrees for the heavy UI) **built the entire plant/insect
identity-map ecology + the Simulator** — the big design session the last handoff
starred as "next." It's all committed to local `master` and green, but see ⭐.

Exhaustive durable detail is in auto-memory **`wonder-plant-insect-ecology`** (the
full model, every commit, the QA rounds, what's deferred to v2). This file is the
short resume pointer — read the memory for depth, don't make me restate it here.

## ⭐ START HERE — the work is committed but **NOT PUSHED / NOT DEPLOYED**

`origin/master` is still at `9d78f33` (last session's handoff commit). **All 20
commits** of the ecology arc — `d11f7a4 … a731294` — live only on local `master`
(confirmed with `git fetch`; local is ahead by 20). Deploy is auto-on-push, so
**blainebooher.com/wonder/ is still the pre-ecology build.** The session was
cleared before the final push.

**First action on resume (with Blaine's go-ahead — push is his call):**
```
npm run check && npx vitest run && npm run build   # all green as of this handoff
git push origin master                             # → triggers the live deploy
```
Nothing is broken; it just hasn't shipped. The memory file's "deploying" language
was written ahead of the push that never landed.

## What was built this session (local master, `d11f7a4..a731294`)

The whole arc, one session, per plan
`docs/superpowers/plans/2026-07-21-plant-insect-ecology-core.md`. Headlines:

- **Ecology core** (`src/life/idmap.ts`, `swarm.ts`) — John-Holland ECHO-style
  identity-map tags; a swarm = one moving cloud + a ~10-genome internal pool;
  feeding = adaptive metabolism (nectar-gated, energy = match quality);
  facultative pollination; spatial camouflage; **gentle** predation (population
  down, never death — peaceful pillar holds); behaviour genes; divergence → cousins.
- **The Simulator** (`src/game/simulator.ts`, `?sim=1`) — dev-bench to place
  flowers/swarms and watch clouds adapt.
- **Swarms live in real islands** (`src/game/swarms.ts` `SwarmLayer`) — flower maps
  on blooming species; swarms home, adapt, pollinate → the **reciprocal boom**
  (`flora.pollinateSpread`, bounded); cosmetic `Pollinators` retired.
- **17 generative insect body forms** (`src/render/insectSprites.ts`) — plan rolled
  from the behaviour-gene hash (stable for life), colour/pattern from the sensor map
  so the flower's hues land pixel-by-pixel as it adapts; 12× examine portrait; each
  swarm gets a codex name.
- **Reward surfaces / discoverability** — field-guide entries, first-meeting HUD cue
  + murmur, journal swarm shelf, boom/cousin events witnessed (murmur + journal +
  memory, no rng draws so determinism holds), `C` web names clouds + draws the
  insect, `V` overlay rings swarms by exposure, `G` ledger plots per-swarm match-%
  over time, **court-a-cloud by sowing its flower** (taught + murmur'd), Tab-menu
  door to the Simulator.
- **Tech doc** `docs/superpowers/2026-07-22-plant-insect-ecology-tech.md` — full
  math, worked feeding/energy numbers, all three "reproduction" senses, and **§10 a
  one-table index of every tunable constant** (file + up/down effect). **Read this
  before retuning anything.** Screenshots under `docs/superpowers/images/plant-insect/`.

## Open threads (audit minors + judgement calls — none blocking)
- **Warm booms can read as monoculture slabs** — a well-matched pair blooms into a
  large single-hue patch; may want visual break-up.
- **Standing-zoom insects are 2–3px** — the 17 forms only reveal on Z-lean / examine.
  Intentional for now; flagged as a possible readability lever.
- **v2 deferred (not started):** dual insect map, evolving-predator Red-Queen,
  obligate pollination, critter specialization *onto* swarms (audit noted it's not
  yet wired), scrubbable timeline, the other World-Lab doors (deep-time preview, the
  Forge). See the memory file's "Deferred (v2)" list.

## Verify on resume
```
npm run check   # tsc, exit 0 (verified this handoff)
npx vitest run  # 435 green, 77 files (verified — reachability.test is ~11s, that's normal)
npm run build   # succeeds, dist ~264 kB js
```
Determinism holds: sim/gen use no `Math.random`/`Date.now`; swarm selection and
boom/cousin events run off seeded streams, event surfacing draws no rng.

**Build model that worked:** Opus inlined pure-logic plans (TDD); dispatched Fable
in worktrees for heavy UI/integration and all 3 QA passes; verified each
independently (diff read + check/test/build + eyeball `npm run shot` PNGs) and
cherry-picked to master. Blaine drove live and asked to "go all the way with a QA
check loop." Related memory: `wonder-plant-insect-ecology`, `wonder-playtest-fixes`,
`wander-ecology-engine`.
